<DockPanel x:Class="TTS.CardTool.UI.View.DeckView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:prism="http://prismlibrary.com/"
           prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel DockPanel.Dock="Right" Margin="5" Orientation="Vertical">
        <Button Content="Load Decklist" Margin="0 0 0 3" Command="{Binding OpenListCommand}"/>
        <Button Content="Generate TTS Data" Margin="0 3 0 3" Command="{Binding CreateImageCommand}"/>
    </StackPanel>

    <TextBox DockPanel.Dock="Left" Margin="5" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
</DockPanel>
